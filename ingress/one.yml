apiVersion: apps/v1
kind: Deployment
metadata:
  name: ccit-dep
spec:
  replicas: 2
  selector:
    matchLabels:
      app: ccit
  template:
    metadata:
      labels:
        app: ccit
    spec:
      containers:
      - name: cont-1
        image: cloudintelugu/ccitimage:v3
        ports:
        - containerPort: 80
        env:
        - name: TITLE
          value: "CCIT"
---
apiVersion: v1
kind: Service
metadata:
  name: ccit-svc
spec:
  type: NodePort
  ports:
  - port: 80
  selector:
    app: ccit
